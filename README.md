# PR #839 evidence

Screenshots attached to the PR thread for #839 (cmlanca's UX review follow-up).
This branch is not merged into `main` -- it exists only so the images have a
URL. Delete it once the PR lands.
