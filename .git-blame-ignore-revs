# Commits whose only meaningful effect was line-ending normalization
# (CRLF -> LF). Ignore them in `git blame` and GitHub blame views so the
# history points at real changes:
#
#   git config blame.ignoreRevsFile .git-blame-ignore-revs

# Scope CLI exit handling to atlas-chat (also rewrote atlas/atlas_client.py
# line endings while editing it).
ac9c325ba3294df9380366e5817047abeae7ad7f

# Fix release follow-ups and lockfile guardrails (also rewrote
# atlas/infrastructure/events/cli_event_publisher.py and
# atlas/tests/test_config_manager.py line endings while editing them).
f3117cd8ada6e4faba7a0aaadd1c6ec6bc76a98a
