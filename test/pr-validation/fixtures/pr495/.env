# PR #495 test fixtures: help markdown migration
# Points HELP_CONFIG_FILE at the custom override markdown file so the
# validation script can verify user overrides take priority over the
# shipped atlas/config/help.md default.
HELP_CONFIG_FILE=test/pr-validation/fixtures/pr495/custom-help.md
