# Custom Help Override

This is a user-supplied help override used by PR #495 validation.

If `HELP_CONFIG_FILE` points to this file, `/api/config` must return this
content in the `help_content` field instead of the shipped default.

## Marker

PR495_FIXTURE_MARKER_DO_NOT_REMOVE
