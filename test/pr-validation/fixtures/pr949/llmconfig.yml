models:
  scripted-mock:
    model_url: "http://127.0.0.1:__MOCK_LLM_PORT__/v1"
    model_name: "openai/scripted-mock"
    api_key: "mock"
    description: "Scripted mock model: returns scripted atlas launch-discovery and launch tool calls"
    compliance_level: "Internal"
    supports_tools: true
