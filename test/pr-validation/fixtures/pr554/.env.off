# PR #554 fixture: Agent Portal feature flag OFF (default).
# Assertions: routes are NOT mounted; service is not instantiated.
DEBUG_MODE=true
FEATURE_PROXY_SECRET_ENABLED=false
FEATURE_AGENT_PORTAL_ENABLED=false
